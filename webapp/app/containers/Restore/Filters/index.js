//
// Container filters page restore
//

import { connect } from 'react-redux';
import { RestoreFilters } from 'components/Restore/Filters';

function mapStateToProps() {
  return {
  };
}

function mapDispatchToProps() {
  return {
  };
}

export default connect(
  mapStateToProps,
  mapDispatchToProps,
)(RestoreFilters);
