//
// Container filters page software
//

import { connect } from 'react-redux';
import SoftwareFilters from 'components/Software/Filters';

function mapStateToProps() {
  return {
  };
}

function mapDispatchToProps() {
  return {
  };
}

export default connect(
  mapStateToProps,
  mapDispatchToProps,
)(SoftwareFilters);
