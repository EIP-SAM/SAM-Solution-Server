//
// Container dashboard by user page
//

import { connect } from 'react-redux';
import DashboardByUser from 'components/DashboardByUser';

function mapStateToProps() {
  return {
  };
}

function mapDispatchToProps() {
  return {
  };
}

export default connect(
  mapStateToProps,
  mapDispatchToProps,
)(DashboardByUser);
