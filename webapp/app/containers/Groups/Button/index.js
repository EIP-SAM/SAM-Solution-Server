//
// Container button groups
//

import { connect } from 'react-redux';
import GroupsButton from 'components/Groups/Button';

function mapStateToProps() {
  return {
  };
}

function mapDispatchToProps() {
  return {
  };
}

export default connect(
  mapStateToProps,
  mapDispatchToProps,
)(GroupsButton);
